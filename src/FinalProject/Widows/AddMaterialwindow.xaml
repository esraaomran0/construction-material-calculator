<Window x:Class="FinalProject.Widows.AddMaterialwindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FinalProject.Widows"
        mc:Ignorable="d"
        Title="Add Material" Height="320" Width="350"
        Background="WhiteSmoke">

    <Window.Resources>
        <Style TargetType="TextBox">
            <EventSetter Event="TextChanged" Handler="TextBox_TextChanged"/>
        </Style>
        <Style TargetType="ComboBox">
            <EventSetter Event="SelectionChanged" Handler="ComboBox_SelectionChanged"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="15">
        <StackPanel VerticalAlignment="Center">

            <DockPanel Height="35"  >
                <TextBlock Width="90" DockPanel.Dock="Left" Foreground="Black">Name</TextBlock>
                <TextBox x:Name="materialname"/>
            </DockPanel>

            <DockPanel Height="35"  >
                <TextBlock Width="90" DockPanel.Dock="Left" Foreground="Black">Category</TextBlock>
                <ComboBox x:Name="categoryname" ItemsSource="{Binding MaterialType}"/>
            </DockPanel>

            <DockPanel Height="35" Margin="0,5">
                <TextBlock Width="90" DockPanel.Dock="Left" Foreground="Black">Unit</TextBlock>
                <ComboBox x:Name="unitindecation">
                    <ComboBoxItem>Ton</ComboBoxItem>
                    <ComboBoxItem>kg</ComboBoxItem>
                    <ComboBoxItem>m³</ComboBoxItem>
                    <ComboBoxItem>m²</ComboBoxItem>
                    <ComboBoxItem>Liter</ComboBoxItem>
                    <ComboBoxItem>Piece</ComboBoxItem>
                </ComboBox>
            </DockPanel>

            <DockPanel Height="35" Margin="0,5">
                <TextBlock Width="90" DockPanel.Dock="Left" Foreground="Black">Price/Unit</TextBlock>
                <TextBox x:Name="unit_price"/>
            </DockPanel>

            <Button x:Name="saave"                                         
                    Click="Button_Save">Save</Button>
            <Button  
                    Click="Button_Cancel">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>